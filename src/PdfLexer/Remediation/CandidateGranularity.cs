namespace PdfLexer.Remediation;

/// <summary>Structured-text candidate granularity supported by preview bindings and anchors.</summary>
public enum Granularity
{
    Character,
    Word,
    Line,
    Paragraph
}
